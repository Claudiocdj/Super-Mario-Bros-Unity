using System;
using UnityEngine;

public class FloorDetection : CollisionDetection
{
    //public event Action Collided = delegate { };

    private void OnTriggerEnter2D(Collider2D collision)
    {
     //   OnCollision( collision );
    }
}
